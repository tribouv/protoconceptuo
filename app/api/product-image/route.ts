import {fetchTrusted,mediaHosts} from '@/lib/editor/remote';
export async function GET(request:Request){try{const url=new URL(request.url).searchParams.get('url');if(!url)throw new Error('Photo manquante.');const result=await fetchTrusted(url,mediaHosts,6_000_000);if(!/^image\/(jpeg|png|webp)(;|$)/.test(result.type))throw new Error('Photo JPEG, PNG ou WebP requise.');return new Response(result.bytes,{headers:{'Content-Type':result.type,'Cache-Control':'private, max-age=300','X-Content-Type-Options':'nosniff'}})}catch(e){return Response.json({error:e instanceof Error?e.message:'Photo indisponible.'},{status:422})}}
